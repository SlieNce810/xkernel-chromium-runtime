# ============================================================================
# 中电杯 · 赛题六（x-kernel 的 Chromium 运行环境构建与优化）
# 仓库忽略规则
# ============================================================================

# ── 过程产物 ────────────────────────────────────────────────────────────────
# AGENTS.md 明确：「tmp/ is disposable local working output.
# Do not treat it as deliverable evidence.」
# 其中还含 tmp/xk-docs（x-kernel 的浅克隆，自带 .git，会破坏外层仓库）与
# tmp/xk-dump（源码快照），一律不入库。
/tmp/

# ── Python ─────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]

# ── 编辑器 / 操作系统 ───────────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# ── 本地临时文件 ────────────────────────────────────────────────────────────
*.orig
*.rej
