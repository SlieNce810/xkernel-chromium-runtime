# T490 证据索引（2026-09-21）

> 环境契约（所有会话一致）：T490 / Ubuntu 26.04 / **QEMU 10.2.1** / `kplat-aarch64` + `qemu_defconfig`
> / **纯 TCG（`ACCEL=n`，无 KVM/HVF）** / `MEM=4g SMP=4` / 设备含 `virtio-gpu-pci` + `virtio-keyboard-pci` + `virtio-mouse-pci`
> 每个会话目录含：`console.log`（串口全量）、`cmd.txt`（QEMU 实际命令行）、`env.txt`（宿主机指纹）、`manifest.txt`、`screenshots/*.ppm`
> 截图方式：QEMU monitor `screendump`（赛题唯一认可）

| # | 会话 | 目的 | 关键结论 | 对应验收 |
|---|---|---|---|---|
| 1 | `_t490-diag1` | 首次在 T490 上建立 DRM 通路基线 | `drmprobe`：card0 `open+ioctl(VERSION)` **FULL_OK**（`simpledrm 1.0.0`）；`drmGetVersion -> NULL/EFAULT`；`drmGetDevices2 -> 0/ENOENT` | P0 基线 / P1 基线 |
| 2 | `_t490-exp-builtin` | 试探 libseat builtin 后端 | `No backend matched name 'builtin'`；**seatd.log 首次暴露 VT 缺口**（`Could not open VT for client`） | 缺口 G2 |
| 3 | `_t490-v7-tty0-shim` | 构造 `/dev/tty0` + shim v1 | `mknod 4:0` 得 **ENXIO**（无驱动）→ 改符号链接；shim v1 仅拦 `open_device`，失败在更早阶段 | G2 |
| 4 | `_t490-v8-shim2` | shim v2（全 API 接管） | `open_seat()` 被调用 ✓ 但 `enable_seat` 未触发 → weston 报 fatal | G2 |
| 5 | `_t490-v9-shim3` | shim v3（有效 `get_fd` + 全量日志） | **关键转折**：`libseat: session control granted` 后 weston **从未调用 `libseat_open_device`** → 失败不在 libseat 层 | G1 定位起点 |
| 6 | `_t490-v10-strace` | 注入 strace 抓 syscall | strace 装上但 **x-kernel 无 ptrace，不可用**；同轮拿到 `drmprobe v2` 基线 | 工具边界 |
| 7 | `_t490-v11-sysfs` | 首次预置 sysfs 文件 | 文件在 guest 内**不可见** → 证明 `/sys` 被 memfs 挂载覆盖 | 坑 #3 |
| 8 | `_t490-v12-shim4` | shim v4（**延迟回调**）+ 运行时伪造 sysfs | 伪造 sysfs 生效（`/sys/class/drm/card0/dev` 可读）；`realpath` 诊断正常 | G1/G2 |
| 9 | `_t490-v13-sysfs2` | 完整伪造树（libdrm faux + libudev） | `drmGetDevices2 -> 0`（0 = 成功但 0 设备，**判定修正**）；libdrm 非阻塞 | P1-B2 |
| 10 | `_t490-v14-realpath` | realpath 与 readlink 诊断 | `readlink(subsystem) -> ../../faux`、`realpath` 正常，仍 0 设备 → 疑点在 libdrm 内部 | P1-B2 |
| 11 | `_t490-p0-fix` | **P0 补丁验收** | `drmGetVersion(fd) -> 0x40c2fb0 errno=0`（基线 NULL/14）、`simpledrm 1.0`、panic=0 | **P0-A1~A5 ✅** |
| 12 | `_t490-v15-libudev` | drmprobe v3（libudev 探针） | **libudev 查询全通**：`devnode=/dev/dri/card0`、`sysnum=0`、`devnum=57856` → 失败点在 weston 传参 | G1 定位 |
| 13 | `_t490-v16-final` | **主攻：shim + `--drm-device=card0`** | 🏆 **`Output 'Virtual-1' enabled with head(s) Virtual-1`**（compositor 完整运行）；客户端因 **SCM_RIGHTS fd 传递**崩溃而退出 | **P1-B4/B5 ✅ / B6/B7 ❌** |

## 关键证据行速查

| 主题 | 位置 |
|---|---|
| P0 修复前后对比 | `_t490-diag1` vs `_t490-p0-fix` 的 `console.log` → `drmGetVersion` 行 |
| weston 首次成功启用输出 | `_t490-v16-final/console.log` → `Output 'Virtual-1' enabled`（约 239 行） |
| G4（SCM_RIGHTS）现场 | `_t490-v16-final/console.log` → `libwayland: file descriptor expected, object (10), message create_pool(nhi)`（约 253 行） |
| seatd VT 缺口现场 | `_t490-exp-builtin/console.log` → `seatd.log tail` 段 |
| libudev 成功证据 | `_t490-v15-libudev/console.log` → `new_from_subsystem_sysname(drm, card0) -> 0x23150` 及其属性 |
| 纯 TCG 自证 | 任一会话 `cmd.txt`：无 `-accel kvm/hvf`（含 `-accel tcg` 不算违规）；`AArch64` guest + x86_64 宿主 |

## 归档说明
- 归档时间：2026-09-21 12:20（收口）；来源：T490 `~/xk6/evidence/`
- 归档方式：`tar czf` 单文件传输 + sha256 校验（`f531dbfe35398908860c8453cd4f8344ee139ea63eb7e9227bd2eb14d7f238d9`）
- 规模：13 个会话 / 96 张 PPM / 每会话精选 2 张 PNG（共 26 张）/ `console.log` 13 份 / 约 119MB（含 09-20 WSL2 六轮）
- 每会话目录结构：`console.log`、`cmd.txt`、`env.txt`、`manifest.txt`、`screenshots/*.ppm` + `*.png`（精选帧）
- T490 侧清理（同日）：删除 `/tmp/wsl-weston.img`(4G 稀疏)、`/tmp/disk-t490-built.img`、musl 工具链 tar 等中间产物；
  `/tmp` 降至 24K、`~/xk6/tmp` 2.8M；**两个冻结镜像 sha256 未变**，无残留 QEMU 进程
